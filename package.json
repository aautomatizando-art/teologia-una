{
  "name": "esp32-encoder-relay",
  "version": "2.0.0",
  "description": "Relay WebSocket server — ESP32 Encoder Dashboard",
  "main": "encoder/relay/server.js",
  "scripts": {
    "start": "node encoder/relay/server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18"
  }
}
